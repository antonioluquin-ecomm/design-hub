# Notas — Maqueta del Portal de Gestión de Pedidos

## Estado

Andamiaje creado, sin contenido real todavía. El portal de gestión de pedidos es una **herramienta externa** (tipo VTEX Admin / Order Manager) sin código fuente accesible — no se puede leer su HTML/CSS directamente.

## Qué falta para empezar a maquetar contenido real

- [ ] URL del portal real, para navegarla en el preview del browser y capturar su estructura (layout, tablas, filtros, acciones por pedido)
- [ ] O, alternativamente, capturas de pantalla de las pantallas relevantes
- [ ] Confirmar con el usuario cuáles son las pantallas prioritarias a replicar primero (ej: listado de pedidos, detalle de pedido, estados/filtros)

## Una vez con el insumo

Reemplazar este placeholder (`index.html`) por la maqueta real, usando los tokens de `../../design-system/tokens.css`. Registrar en `../../docs/cross-references.md` cualquier doc de proceso/pedidos que la maqueta empiece a reflejar.
